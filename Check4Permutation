"""
This file check is there's a permutation exists in a string.
This check is valid for a character and is not case-sensitive.
"""

def bool_permutation(str1, str2):
  
  if len(str1) != len(str2):
    return False
    
  else:
    return ''.join(sorted(str1.upper())) == ''.join(sorted(str2.upper()))
